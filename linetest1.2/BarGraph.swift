//
//  BarGraph.swift
//  LINES
//
//  Created by david crabtree on 5/9/17.
//  Copyright © 2017 david crabtree. All rights reserved.
//

import Foundation
import UIKit

/*
class BarGraph: NSObject {
    
    //MARK: Properties
    var name: String
    var waitAt11: Int
    var waitAt12: Int
    var waitAt1: Int
    var waitAt2: Int
    var waitAt3: Int
    var waitAt4: Int
    var waitAt5: Int
    var waitAt6: Int
    var waitAt7: Int
    var waitAt8: Int
    var waitAt9: Int
    var waitAt10: Int
    
    struct PropertyKey {
        
        static let name = "name"
        static let waitAt11 = "waitAt11"
        static let waitAt12 = "waitAt12"
        static let waitAt1 = "waitAt1"
        static let waitAt2 = "waitAt2"
        static let waitAt3 = "waitAt3"
        static let waitAt4 = "waitAt4"
        static let waitAt5 = "waitAt5"
        static let waitAt6 = "waitAt6"
        static let waitAt7 = "waitAt7"
        static let waitAt8 = "waitAt8"
        static let waitAt9 = "waitAt9"
        static let waitAt10 = "waitAt10"
        
    }
    
    //MARK: Initialization
    
    init?(name: String, waitAt11: Int, waitAt12: Int, waitAt1: Int, waitAt2: Int, waitAt3: Int, waitAt4: Int, waitAt5: Int, waitAt6: Int, waitAt7: Int, waitAt8: Int, waitAt9: Int, waitAt10: Int) {
        
    //Initialize stored properties
        self.name = name
        self.waitAt11 = waitAt11
        self.waitAt12 = waitAt12
        self.waitAt1 = waitAt1
        self.waitAt2 = waitAt2
        self.waitAt3 = waitAt3
        self.waitAt4 = waitAt4
        self.waitAt5 = waitAt5
        self.waitAt6 = waitAt6
        self.waitAt7 = waitAt7
        self.waitAt8 = waitAt8
        self.waitAt9 = waitAt9
        self.waitAt10 = waitAt10
    }
    
    func encode(with aCoder: NSCoder) {
        aCoder.encode(name, forKey: PropertyKey.name)
        aCoder.encode(waitAt11, forKey: PropertyKey.waitAt11)
        aCoder.encode(waitAt12, forKey: PropertyKey.waitAt12)
        aCoder.encode(waitAt1, forKey: PropertyKey.waitAt1)
        aCoder.encode(waitAt2, forKey: PropertyKey.waitAt2)
        aCoder.encode(waitAt3, forKey: PropertyKey.waitAt3)
        aCoder.encode(waitAt4, forKey: PropertyKey.waitAt4)
        aCoder.encode(waitAt5, forKey: PropertyKey.waitAt5)
        aCoder.encode(waitAt6, forKey: PropertyKey.waitAt6)
        aCoder.encode(waitAt7, forKey: PropertyKey.waitAt7)
        aCoder.encode(waitAt8, forKey: PropertyKey.waitAt8)
        aCoder.encode(waitAt9, forKey: PropertyKey.waitAt9)
        aCoder.encode(waitAt10, forKey: PropertyKey.waitAt10)
}

    required convenience init?(coder aDecoder: NSCoder) {
        let name = aDecoder.decodeObject(forKey: PropertyKey.name) as! String
        let waitAt11 = aDecoder.decodeObject(forKey: PropertyKey.waitAt11) as! Int
        let waitAt12 = aDecoder.decodeObject(forKey: PropertyKey.waitAt12) as! Int
        let waitAt1 = aDecoder.decodeObject(forKey: PropertyKey.waitAt1) as! Int
        let waitAt2 = aDecoder.decodeObject(forKey: PropertyKey.waitAt2) as! Int
        let waitAt3 = aDecoder.decodeObject(forKey: PropertyKey.waitAt3) as! Int
        let waitAt4 = aDecoder.decodeObject(forKey: PropertyKey.waitAt4) as! Int
        let waitAt5 = aDecoder.decodeObject(forKey: PropertyKey.waitAt5) as! Int
        let waitAt6 = aDecoder.decodeObject(forKey: PropertyKey.waitAt6) as! Int
        let waitAt7 = aDecoder.decodeObject(forKey: PropertyKey.waitAt7) as! Int
        let waitAt8 = aDecoder.decodeObject(forKey: PropertyKey.waitAt8) as! Int
        let waitAt9 = aDecoder.decodeObject(forKey: PropertyKey.waitAt9) as! Int
        let waitAt10 = aDecoder.decodeObject(forKey: PropertyKey.waitAt10) as! Int
        
        self.init(name: name, waitAt11: waitAt11, waitAt12: waitAt12, waitAt1: waitAt1, waitAt2: waitAt2, waitAt3: waitAt3, waitAt4: waitAt4, waitAt5: waitAt5, waitAt6: waitAt6, waitAt7: waitAt7, waitAt8: waitAt8, waitAt9: waitAt9, waitAt10: waitAt10)
        
    }
    
    
}
 */
 

